# Рекомендація: Зайва залежність PyYAML

## Проблема

У `pyproject.toml` вказано залежність:

```toml
dependencies = [
    "msgspec>=0.18.0",
    "pyyaml>=6.0.0",
]
```

Однак у коді **PyYAML не використовується**. Усі YAML-операції виконуються через `msgspec.yaml`:

```python
import msgspec

# В коді
yaml_bytes = msgspec.yaml.encode(data)
msgspec.yaml.decode(content)
```

`msgspec.yaml` внутрішньо використовує PyYAML як backend, тому залежність потрібна, але **опціонально**.

## Перевірка

`msgspec` сам декларує PyYAML як опціональну залежність:

```
msgspec[yaml]  # включає pyyaml
```

## Рекомендація Зробити YAML опціональним

```toml
dependencies = [
    "msgspec>=0.18.0",
]

[project.optional-dependencies]
yaml = [
    "msgspec[yaml]>=0.18.0",
]
```

Тоді користувачі встановлюють:
```bash
pip install easy_file[yaml]
```

## Пріоритет

🟢 **Низький** — поточна конфігурація працює, але варто розглянути спрощення.
