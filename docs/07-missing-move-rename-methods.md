# Рекомендація: Відсутні методи `move()` та `rename_to()`

## Проблема

Клас `File` надає метод `copy()`, але не має методів для:
- Переміщення файлу (`move`)
- Перейменування файлу (`rename`)

Хоча `pathlib.Path` має метод `rename()`, він:
1. Не створює батьківські директорії автоматично
2. Повертає `Path`, а не `File`
3. Може не працювати між різними файловими системами

## Рекомендація


```python
import shutil

def move(self, target_path: str | pathlib.Path) -> "File":
    """Move this file to the target path."""
    target = File(target_path)
    target.parent.mkdir(parents=True, exist_ok=True)
    shutil.move(str(self), str(target))
    return target
```

## Пріоритет

🟡 **Середній** — корисна функціональність для повноти API.
