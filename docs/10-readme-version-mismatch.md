# Рекомендація: Невідповідність версій у README

## Проблема

У README.md у прикладах вказано застарілу версію:

```python
config.dump_json({"name": "Easy File", "version": "0.2.0"})
```

Поточна версія проєкту: **0.4.0** (з `pyproject.toml` та `__init__.py`)

## Місця для виправлення

1. Quick Start секція:
   ```python
   config.dump_json({"name": "Easy File", "version": "0.2.0"})
   ```

2. Typed Deserialization секція:
   ```python
   config.dump_json({"name": "Easy File", "version": "0.2.0", "debug": True})
   ```

3. Async Methods секція:
   ```python
   await config.dump_json_async({"name": "Easy File", "version": "0.2.0"})
   ```

## Рекомендація
використати версію, яка не залежить від поточної версії пакету:

```python
config.dump_json({"name": "My App", "version": "1.0.0"})
```

## Пріоритет

🟢 **Низький** — косметична проблема, але впливає на професійність документації.
